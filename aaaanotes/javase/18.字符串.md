# 1.类
java,lang.String类
构建字符串对象，双引号就行
`String name = "zhangsan`";
转义字符： \"在想转移的字符前边
\' ,\t,\n,\\\
`String s = new String("abc");`创建几个对象？

- 如果常量池没有`"abc"`：**2 个对象**（常量池 1 个 + 堆 new 出来 1 个）
- 如果常量池已经存在`"abc"`：**只创建堆里 1 个对象**
# 2.关系
字符串：字符的数组
字节
字符
数组
除ASCII码以外的字符是三个字节
# 3.方法
## 1.拼接
"a"+"b"[16. Object类](16.%20Object类.md)里的`hashCode`
拼接数字，加号前或者后边是字符串，后边自动变成字符串
`但是1+2+"abc"就变成了3abc`，会创建新的字符串
`concat方法`
## 2.比较
equals方法，每一个字符是否相等，包括大小写
`equalsIgnoreCase忽略大小写`
`compareTo`比较大小，正数为大，负数为小，0是相等
## 3.截断
`substring`(0,3)只截取0，1，2三个位置，前包含后不包含
`substring(0,"Hello".length())`,就截取到hello
只传递一个参数，表示从该位置截取到最后
split()进行分解，中间参数表示在哪里解开
trim()去掉字符串首尾空格方法
## 4. 替换
replace(原字符串，字符串)
`replaceAll("world|zhangsan","java")`

## 5.大小写替换
`1.toLowerCase`
`2.toUpperCase`
## 6.查找
`toCharArray`
`getBytes 得到字节码`
`charAt(1)查询指定字符`
`indexOf()找参数的第一个位置`
`lastIndexOf()`找最后一次出现的位置
contains（）是否包含指定字符串 
`isEmpty(0`)判断字符串是否为空，空格不是空
# 4.`StringBuilde`r类
构建字符串
其中有个方法append()拼接字符串
.length()
.reverse()
insert(1,b),位置，内容

