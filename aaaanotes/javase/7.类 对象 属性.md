
## 1. 类表示归纳，对象为具体的事物
类class，引用数据类型
构建对象：new 类名（）；
类是对象的模板，对象是类的实例
对象是将内存地址赋给了变量，所以变量引用了内存的对象。
空对象：   = null；
## 2.属性
属性类型 属性名称 = 属性值
如果在类里声明属性时进行初始化，所有对象共同享有。
默认初始化。0   0.0    false    
char = 空字符
引用数据类型 = null
变量只能在当前大括号内使用，
而属性不仅在当前类中使用，随对象在其他范围使用。
变量使用前必须初始化。

补：
1.类里只能写变量定义和方法定义，不能写执行语句