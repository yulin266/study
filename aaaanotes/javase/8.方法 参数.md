### 1 方法
方法类型    方法名 = 类 .方法；
使用外部数据控制方法内部,方法参数
参数个数,参数类型,参数顺序
### 2可变参数
参数类型...参数名称         可以传好几个
如果还包含其他含义的参数,需要把可变参数放到最后
### 3 参数的使用:值传递
这个结果出来还是10
栈前方法先进,后方法后进先执行先弹出,
压栈,弹栈
上边方法执行完之后弹栈,所以main函数里还是10.
而引用数据类型在堆内存里,


引用对象会随着方法改变,从原来的张三变成经理方法的李四
这是覆盖,而上边是建立个新的内存
## 4.构造方法
`JVM自动添加构造方法`
基本语法      类名（）{}
1.没有void关键字。
2.方法名类名完全相同
3.可以传参
对象不能调用构造方法。
4.手写了有参构造方法，虚拟机不会提供无参的构造方法