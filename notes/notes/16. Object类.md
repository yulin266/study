# 1.常见类
Java.lang.Object : 对象[7.类 对象 属性](7.类%20对象%20属性.md)
父类继承了object类，子类不在继承object，而是当作超类
# 2.常见方法
`1.obj.toString（）`将对象变成字符串，默认打印内存地址[3.顺序内存](3.顺序内存.md)
重写这个方法
2.`obj.hashCode()`获取对象的内存地址
3.obj.equals(new Person())判断两个对象是否相等，默认比较内存地址
4.obj.getClass()获取对象类型