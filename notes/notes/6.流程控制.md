
## 1.顺序执行
先后顺序执行
## 2.分支执行
`int i = 10;`
`switch(i){`
`case 10 :`
     `sout10;`
`case 20 :`
    `sout20;`
` default:`
     `sout00; 
`}`
break跳出整个循环。
## 3.循环执环
while
do  while至少执行一次
for
break,跳出整个大循环，不再执行循环操作
continue 跳过当前循环，会执行之后的循环，
## 4.`println` 换行
	print不换行