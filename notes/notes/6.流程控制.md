
# 1.顺序执行
先后顺序执行
# 2.分支执行
`int i = 10;`
`switch(i){`
`case 10 :`
     `sout10;`
`case 20 :`
    `sout20;`
` default:`
     `sout00; 
`}`
break跳出整个循环。
# 3.循环执环
## 1.while
## 2.do  while至少执行一次
## 3.for
1.  for(初始化; 循环条件; 变量更新){ //循环体代码 }
`2.  public class Test { public static void main(String[] args) { int[] arr = {10,20,30,40}; // i代表数组下标索引`
	`for(int i = 0; i < arr.length; i++){` 
		`System.out.println("下标"+i+"，值="+arr[i]); }`
`}` 
`}`
 3增强for循环
 `int[] arr = {10,20,30,40}; // num 依次接收数组中每一个元素 for(int num : arr){`
	  `System.out.println(num);` 
  `}`
## `4.关`键字
break,跳出整个大循环，不再执行循环操作
continue 跳过当前循环，会执行之后的循环，

# 4.`println` 换行
	print不换行